export interface Building {
  id: number;
  buildingName: string;
  totalFloor: number;
  location: string;
}

export interface BuildingResponse {
  id: number;
  buildingName: string;
  totalFloor: number;
}
